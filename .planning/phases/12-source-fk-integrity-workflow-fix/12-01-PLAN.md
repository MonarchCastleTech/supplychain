---
phase: 12-source-fk-integrity-workflow-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/source-fk-integrity.test.mjs
  - tests/workflow-timestamp-quote.test.mjs
  - js/data/index.js
  - js/ui/index.js
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]
must_haves:
  truths:
    - "The 3 swift-payments FK references (profiles C, SAN, 601628.SS) gain a reachable real source link"
    - "resolveSourceId returns the verbatim pre-existing SWIFT source for swift-payments and null for every orphan id"
    - "The 6 orphan refs (novo-sustainability, amgn-products, txn-products, klac-products, tmo-business, gild-products) stay at the honest Unknown floor"
    - "Zero fabricated sources: every resolved FK target id pre-existed as a real source in the dataset"
    - "The frozen data/ JSON and served data/top100-map.js are unchanged (resolver is code-level)"
  artifacts:
    - path: "js/data/index.js"
      provides: "resolveSourceId + CANONICAL_SHARED_SOURCES exports"
      contains: "CANONICAL_SHARED_SOURCES"
    - path: "tests/source-fk-integrity.test.mjs"
      provides: "Resolved-count, orphan-Unknown, zero-fabrication, contract-intact assertions"
      min_lines: 40
    - path: "tests/workflow-timestamp-quote.test.mjs"
      provides: "INFRA-01 yml quote-fix assertion (consumed by plan 12-02)"
      min_lines: 8
  key_links:
    - from: "js/ui/index.js"
      to: "resolveSourceId"
      via: "sourceIndex build at ~L340 and ~L657"
      pattern: "resolveSourceId"
    - from: "js/data/index.js graphForMode"
      to: "resolveSourceId"
      via: "sourceIndex build at ~L167"
      pattern: "resolveSourceId"
---

## Phase Goal

**As an** investor inspecting a profile, **I want to** see a real, reachable source link on a figure whose dangling source FK actually matches a real existing source, **so that** I can trust the citation instead of hitting an opaque Unknown.

<objective>
Add a pure, profile-scoped `resolveSourceId(fkId, ctx)` resolver backed by a `CANONICAL_SHARED_SOURCES` allow-list (keyed on `swift-payments` → the verbatim existing real SWIFT source already cited by 7 sibling profiles) and wire it into every per-profile `sourceIndex` build site. The 3 swift-payments references (27 FK usages across C, SAN, 601628.SS) resolve to a real source; the 6 orphan references (48 usages) return null and stay at the existing Unknown floor. ZERO fabricated sources; frozen `data/` JSON untouched.

Purpose: Reconnect only honestly-resolvable FKs (INTG-01) while keeping unresolvable FKs at the Unknown floor with zero fabrication (INTG-02/03), backed by a registered test gate.
Output: `resolveSourceId` + `CANONICAL_SHARED_SOURCES` in js/data; wiring in js/ui + graphForMode; two new registered test files (one of which, the yml test, is consumed by plan 12-02).
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-fk-integrity-workflow-fix/12-RESEARCH.md
@.planning/phases/12-source-fk-integrity-workflow-fix/12-CONTEXT.md
@.planning/phases/12-source-fk-integrity-workflow-fix/12-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

js/data/index.js — graphForMode sourceIndex build (~L167):
  sourceIndex: Object.fromEntries((p.sources || []).map((s) => [s.id, s])),
js/data/index.js — export block (L235-240): add resolveSourceId, CANONICAL_SHARED_SOURCES to the named exports.

js/ui/index.js — two sourceIndex build sites:
  ~L340: const sourceIndex = Object.fromEntries((profile?.sources || []).map((s) => [s.id, s]));
         const prov = provenanceFor(anchor, { sourceIndex, meta: DATA.meta });
  ~L657: const sourceIndex = Object.fromEntries((p.sources || []).map(s => [s.id, s]));
         const prov = provenanceFor(n, { sourceIndex, meta: DATA.meta });

js/trust/index.js — provenanceFor consumes sourceIndex; returns {tag} (Unknown floor) when fk does NOT resolve. UNCHANGED this plan.

The verbatim canonical source (already present byte-identical in 7 profiles: BAC, HSBC, MUFG, 1398.HK, 601288.SS, 601939.SS, CBA.AX):
  id "swift-payments", title "SWIFT payments network overview", url "https://www.swift.com/payments".

Test shim for loading the served data file (pattern from tests/data-shape.test.mjs):
  const SRC = readFileSync(join(__dirname, "..", "data", "top100-map.js"), "utf8");
  const fn = new Function("window", SRC); const win = {}; fn(win); const DATA = win.SUPPLY_MAP_DATA;
  // DATA.profiles[sym].sources[], .nodes[].sourceId, .links[].sf

The 6 orphan FK ids (no real source anywhere → must return null):
  novo-sustainability, amgn-products, txn-products, klac-products, tmo-business, gild-products
The 3 resolvable refs: swift-payments in profiles C, SAN, 601628.SS (27 FK usages total).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Register both new test files in package.json</name>
  <read_first>package.json (scripts.test line 11)</read_first>
  <files>package.json, tests/source-fk-integrity.test.mjs, tests/workflow-timestamp-quote.test.mjs</files>
  <action>Append `tests/source-fk-integrity.test.mjs` and `tests/workflow-timestamp-quote.test.mjs` to the end of the existing `node --test ...` list in package.json `scripts.test` (do not reorder or remove any of the existing 28 entries). Create both new test files as scaffolds so `npm test` can discover them: source-fk-integrity.test.mjs with at least one placeholder `test(...)` that imports nothing failing, and workflow-timestamp-quote.test.mjs likewise. The real assertions land in Task 1 and in plan 12-02. The yml test file is created here (Wave 0) so plan 12-02 only edits its assertions and the yml.</action>
  <verify>
    <automated>node --test tests/source-fk-integrity.test.mjs tests/workflow-timestamp-quote.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.test contains both new filenames appended after the existing list; all 28 prior entries intact.
    - `npm test` discovers both new files (no "module not found" / no unregistered files).
    - Both scaffold files exist and run green.
  </acceptance_criteria>
  <done>Both test files registered in package.json scripts.test and discoverable by `npm test`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: resolveSourceId + CANONICAL_SHARED_SOURCES with the integrity test gate</name>
  <read_first>js/data/index.js (L130-169 graphForMode, L235-240 exports), js/trust/index.js (provenanceFor Unknown floor ~L50-57), tests/data-shape.test.mjs (window-shim loader, L13-29)</read_first>
  <files>js/data/index.js, tests/source-fk-integrity.test.mjs</files>
  <behavior>
    - resolveSourceId("swift-payments", {}) returns an object whose id/title/url are byte-equal to the verbatim canonical SWIFT source (title "SWIFT payments network overview", url "https://www.swift.com/payments").
    - resolveSourceId(id, {}) returns null for each of the 6 orphan ids (novo-sustainability, amgn-products, txn-products, klac-products, tmo-business, gild-products).
    - resolveSourceId(fkId, { ownSources }) returns ownSources[fkId] when the id is already in the profile's own sources (own source wins).
    - resolveSourceId(null/undefined/"") returns null.
    - Over the full dataset: exactly 27 dangling FK usages (the 3 swift-payments refs in C, SAN, 601628.SS) resolve to a non-null real source; exactly 48 usages (the 6 orphans) resolve to null.
    - Zero fabrication: every id the resolver maps a dangling FK to (swift-payments) already exists as a real source id somewhere in DATA.profiles[*].sources.
    - Contract intact: data/top100-map.js still evaluates via the window shim and still contains the same 75 total dangling-FK usages (resolver is code-level; no data edit).
  </behavior>
  <action>In js/data/index.js add a module-level `CANONICAL_SHARED_SOURCES` constant holding exactly one entry keyed `"swift-payments"` whose value is the verbatim existing source object (id "swift-payments", title "SWIFT payments network overview", url "https://www.swift.com/payments", plus a note field describing it as settlement-infrastructure context). Add a pure exported function `resolveSourceId(fkId, ctx = {})` with conservative rule order: return null for falsy fkId; if `ctx.ownSources` has the id, return that own source; else if `CANONICAL_SHARED_SOURCES` has the id (use hasOwnProperty), return it; else return null. Do NOT add fuzzy/substring/cross-profile matching. Add both `resolveSourceId` and `CANONICAL_SHARED_SOURCES` to the named export block at L235-240. Write tests/source-fk-integrity.test.mjs: load DATA via the window-shim (mirror tests/data-shape.test.mjs), import resolveSourceId/CANONICAL_SHARED_SOURCES from js/data/index.js, and assert every behavior above. Compute the resolved-usage count (27) and orphan-usage count (48) by iterating DATA.profiles[*].nodes[].sourceId and .links[].sf for ids not present in that profile's own sources, then bucketing by whether resolveSourceId returns non-null. Build a realIds Set from all DATA.profiles[*].sources[].id and assert resolveSourceId's swift-payments target id is in realIds (zero fabrication). Reference these as the INTG-01/02/03 gate.</action>
  <verify>
    <automated>node --test tests/source-fk-integrity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - resolveSourceId returns the verbatim canonical SWIFT source for "swift-payments" (id/title/url byte-equal) and null for all 6 orphan ids and for falsy input.
    - Test computes exactly 27 resolved FK usages (3 refs) and 48 Unknown usages (6 refs) from the live dataset.
    - Zero-fabrication assertion passes: the resolved target id pre-exists in the dataset's real source ids.
    - data/ JSON + data/top100-map.js unchanged (test re-evaluates the served file; total dangling usages still 75). `git diff --stat data/` is empty.
    - `node --test tests/source-fk-integrity.test.mjs` exits 0.
  </acceptance_criteria>
  <done>resolveSourceId + CANONICAL_SHARED_SOURCES exported from js/data; integrity test green; data/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire resolveSourceId into the three sourceIndex build sites</name>
  <read_first>js/ui/index.js (L335-345, L650-665), js/data/index.js (L160-169 graphForMode sourceIndex)</read_first>
  <files>js/ui/index.js, js/data/index.js</files>
  <action>At each of the three per-profile sourceIndex build sites — js/ui/index.js ~L340 and ~L657, and js/data/index.js graphForMode ~L167 — after constructing the own-sources map, layer in canonical-shared resolutions: build `ownSources` from the profile's `sources`, seed `sourceIndex` from it, then iterate the profile's nodes (`sourceId`) and links (`sf`); for each dangling FK not already in sourceIndex, call `resolveSourceId(fk, { ownSources })` and, when it returns non-null, add it to sourceIndex under the FK id. Leave `provenanceFor` calls unchanged — they already degrade to the Unknown floor when the FK is absent (orphans). Import `resolveSourceId` into js/ui/index.js (it already imports from ../trust; add the js/data import or extend the existing data import). In js/data/index.js graphForMode, reference the in-module `resolveSourceId` directly. Do not touch js/trust/index.js. Do not edit data/.</action>
  <verify>
    <automated>node --test tests/source-fk-integrity.test.mjs tests/index-ui-integrity.test.mjs tests/ui-provenance-wiring.test.mjs tests/viz-provenance-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All three sourceIndex build sites call resolveSourceId for dangling node/link FKs and inject only non-null real sources (pattern `resolveSourceId` present at js/ui ~L340, ~L657 and js/data graphForMode ~L167).
    - Orphan FKs remain absent from sourceIndex → provenanceFor still returns the Unknown floor (no new code path in js/trust).
    - js/trust/index.js and data/ are unchanged (`git diff --stat js/trust data` empty).
    - The named integrity + provenance-wiring tests pass.
  </acceptance_criteria>
  <done>The 3 swift-payments figures (C, SAN, 601628.SS) gain a reachable real source via the enriched sourceIndex; orphans stay Unknown; provenance/UI wiring tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static data → DOM (badgeHtml) | Resolved source title/url flow into the provenance badge sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | resolved source title/url → badgeHtml `<a>` | accept | badgeHtml already escapeHtml-escapes the title and only emits an `<a>` when url.startsWith("http"); the swift-payments source is a static trusted constant (https URL). No new sink, no untrusted input. |
| T-12-02 | Spoofing | fabricated/unrelated source injected to reduce the dangling count | mitigate | Conservative allow-list keyed only on swift-payments; zero-fabrication test asserts every resolved target id pre-existed as a real source. No fuzzy/cross-profile matching. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase installs zero packages (buildless, node:test built-in). No registry interaction. |
</threat_model>

<verification>
- `node --test tests/source-fk-integrity.test.mjs` green (resolved=27/3, Unknown=48/6, zero fabrication, contract intact).
- Provenance + UI wiring tests green after wiring.
- `git diff --stat data js/trust` empty (frozen data + trust core untouched).
</verification>

<success_criteria>
- resolveSourceId + CANONICAL_SHARED_SOURCES exported from js/data and wired into all three sourceIndex build sites.
- 27 FK usages (3 refs) resolve to the verbatim real SWIFT source; 48 usages (6 orphans) stay at the Unknown floor.
- Zero fabricated sources (asserted); data/ JSON contract unchanged.
- Both new test files registered in package.json scripts.test; named tests exit 0.
</success_criteria>

<output>
Create `.planning/phases/12-source-fk-integrity-workflow-fix/12-01-SUMMARY.md` when done
</output>
