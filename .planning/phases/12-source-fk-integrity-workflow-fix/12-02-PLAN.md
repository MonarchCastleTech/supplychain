---
phase: 12-source-fk-integrity-workflow-fix
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - index.html
  - tests/methodology-wiring.test.mjs
  - .github/workflows/auto-update-data.yml
  - tests/workflow-timestamp-quote.test.mjs
autonomous: false
requirements: [INTG-02, INFRA-01]
must_haves:
  truths:
    - "The Methodology view honestly reports 27 resolved and 48 remaining-Unknown of the 75 previously-dangling FKs"
    - "The auto-update-data.yml timestamp echo redirects to GITHUB_OUTPUT correctly (redirection outside the quoted string)"
    - "The weekly cron, workflow_dispatch, data-validation step, and deploy/commit steps are unchanged"
    - "The full suite (313 baseline + new integrity/workflow cases) stays green"
  artifacts:
    - path: "index.html"
      provides: "Honest resolved/remaining methodology copy"
      contains: "previously-dangling"
    - path: ".github/workflows/auto-update-data.yml"
      provides: "Corrected timestamp echo redirection"
      contains: 'echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"'
    - path: "tests/workflow-timestamp-quote.test.mjs"
      provides: "Asserts the fixed quote and absence of the captured redirection"
      min_lines: 8
  key_links:
    - from: "tests/methodology-wiring.test.mjs"
      to: "index.html methodology copy"
      via: "regex assertions /\\b75\\b/ /\\b27\\b/ /\\b48\\b/"
      pattern: "\\\\b27\\\\b"
    - from: "tests/workflow-timestamp-quote.test.mjs"
      to: ".github/workflows/auto-update-data.yml"
      via: "string-presence assertion"
      pattern: "GITHUB_OUTPUT"
---

## Phase Goal

**As an** investor reading the Methodology disclosure, **I want to** see the honest split of how many previously-dangling source references now resolve versus remain Unknown, **so that** the public integrity record is accurate; **and as** the maintainer, **I want** the weekly auto-update workflow's timestamp step fixed so the pipeline keeps working.

<objective>
Update the Methodology copy (index.html:168) to state the honest resolved/remaining split — KEEP the literal `75` and ADD the resolved count (27 usages / 3 refs) and remaining-Unknown count (48 usages / 6 refs) — and update methodology-wiring assertions to pin all three numbers. Fix the misquoted timestamp echo on auto-update-data.yml line 34 (close the value quote before the redirection and quote `$GITHUB_OUTPUT`), and finalize the workflow-timestamp-quote test created in plan 12-01. Run the full suite as the phase gate, then a human-verify checkpoint of the resolved source link previews.

Purpose: Document the honest remaining-Unknown count in the Methodology view (INTG-02) and fix the workflow quoting bug without disturbing the pipeline (INFRA-01).
Output: Updated methodology copy + assertions, corrected yml line, finalized workflow test, full-suite gate, human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-source-fk-integrity-workflow-fix/12-RESEARCH.md
@.planning/phases/12-source-fk-integrity-workflow-fix/12-VALIDATION.md
@.planning/phases/12-source-fk-integrity-workflow-fix/12-01-SUMMARY.md

<interfaces>
<!-- Exact current strings. Use directly — no exploration needed. -->

index.html:168 (current methodology copy — KEEP 75, ADD 27/48):
  <li>About <strong>75 dangling source references</strong> (source FKs that do not resolve to a listed source) remain in the dataset; figures behind them score at the Unknown floor.</li>

tests/methodology-wiring.test.mjs (~L42-52) currently asserts /\b407\b/, /\b120\b/, /3,?447/, /\b131\b/, /\b75\b/. Add /\b27\b/ and /\b48\b/ alongside the existing /\b75\b/ (do NOT remove /\b75\b/).

.github/workflows/auto-update-data.yml line 34 (current — broken: redirection captured inside the quoted string):
  run: echo "timestamp=$(date +%Y%m%d-%H%M%S) >> $GITHUB_OUTPUT"
Target (value quoted, redirection outside, GITHUB_OUTPUT quoted):
  run: echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"
Unchanged: cron '0 6 * * 1' (L5), workflow_dispatch (L6), data-validation step (L42), commit/push + step-summary steps. The yml is NOT parsed by any gated test other than the new workflow-timestamp-quote test.

tests/workflow-timestamp-quote.test.mjs — created as a scaffold in plan 12-01 (Wave 0), already registered in package.json scripts.test. This plan fills in its assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Honest methodology copy + pinned assertions (INTG-02)</name>
  <read_first>index.html (L168, and L129 for the surrounding qualifier copy), tests/methodology-wiring.test.mjs (L42-52)</read_first>
  <files>index.html, tests/methodology-wiring.test.mjs</files>
  <action>Rewrite the index.html:168 list item so it KEEPS the literal number 75 and ADDS the resolved and remaining counts: state that of 75 previously-dangling source references, 27 now resolve to a real, already-cited source (the SWIFT payments network, shared across global-bank profiles) and the remaining 48 have no real matching source and stay honestly at the Unknown floor. Use the phrase "previously-dangling" so the copy reads as a resolved record, not an unresolved limit. Do not alter the L129 qualifier-tier copy (120 / 3,447 / 407 / 131 must stay). In tests/methodology-wiring.test.mjs, add `assert.match(HTML, /\b27\b/, ...)` and `assert.match(HTML, /\b48\b/, ...)` to the "states the REAL dataset facts" test, keeping the existing `/\b75\b/` assertion and all other number assertions intact.</action>
  <verify>
    <automated>node --test tests/methodology-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - index.html methodology copy contains 75 AND 27 AND 48; reads as resolved/remaining (no longer "About 75 ... remain ... Unknown floor" alone).
    - L129 qualifier copy (120, 3,447, 407, 131) unchanged.
    - methodology-wiring test asserts /\b75\b/, /\b27\b/, /\b48\b/ and exits 0.
  </acceptance_criteria>
  <done>Methodology view honestly documents 27 resolved / 48 remaining-Unknown of 75; methodology-wiring green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix yml timestamp quote + finalize workflow test (INFRA-01)</name>
  <read_first>.github/workflows/auto-update-data.yml (L32-42), tests/workflow-timestamp-quote.test.mjs (scaffold from 12-01)</read_first>
  <files>.github/workflows/auto-update-data.yml, tests/workflow-timestamp-quote.test.mjs</files>
  <action>Edit auto-update-data.yml line 34 to `run: echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"` — close the double-quote immediately after the `$(date ...)` substitution and place the `>> "$GITHUB_OUTPUT"` redirection OUTSIDE the quoted string. Change ONLY this line; leave the cron schedule, workflow_dispatch, the install/download/validation steps (L42 data-validation), and the commit/push + step-summary steps exactly as-is. In tests/workflow-timestamp-quote.test.mjs, readFileSync the yml and assert: it MATCHES the corrected substring `echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"`, and it does NOT MATCH the broken pattern where the redirection is captured inside the quotes (e.g. `%H%M%S) >> $GITHUB_OUTPUT"`). Use plain string-presence assertions (repo convention; no YAML parser).</action>
  <verify>
    <automated>node --test tests/workflow-timestamp-quote.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - auto-update-data.yml line 34 is exactly `run: echo "timestamp=$(date +%Y%m%d-%H%M%S)" >> "$GITHUB_OUTPUT"`.
    - No occurrence of the captured-redirection pattern `%H%M%S) >> $GITHUB_OUTPUT"` remains.
    - cron '0 6 * * 1', workflow_dispatch, L42 validation step, commit/push steps unchanged (`git diff` shows only line 34).
    - workflow-timestamp-quote test asserts the fix and the absence of the broken pattern; exits 0.
  </acceptance_criteria>
  <done>The timestamp step writes to GITHUB_OUTPUT correctly; workflow valid; pipeline steps untouched; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate</name>
  <read_first>package.json (scripts.test)</read_first>
  <files>(none — verification only)</files>
  <action>Run the full registered suite via `npm test`. Confirm the 313 baseline cases plus the new source-fk-integrity and workflow-timestamp-quote cases all pass and that methodology-wiring stayed green with the added 27/48 assertions. If any case is red, fix the offending copy/assertion/yml line (do NOT loosen the zero-fabrication or contract-intact assertions, and do NOT remove the /\b75\b/ pin). Confirm `git diff --stat data` is empty (frozen data contract intact).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 313 baseline + new integrity/workflow cases all green.
    - methodology-wiring asserts 75 + 27 + 48 and passes.
    - `git diff --stat data` empty.
  </acceptance_criteria>
  <done>Full suite green including both new registered test files; data/ contract intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>resolveSourceId reconnects the 3 swift-payments FKs (profiles C, SAN, 601628.SS) to the real SWIFT source; the Methodology copy documents 27 resolved / 48 remaining-Unknown; the auto-update workflow timestamp quote is fixed.</what-built>
  <how-to-verify>
    1. Serve the site locally: `npx http-server -p 8080` (or the repo's existing http-server) and open http://localhost:8080.
    2. Open the Citigroup (C) profile, then Santander (SAN), then China Life (601628.SS). On a financial/settlement demand figure that previously read "Unknown", confirm a "source ↗" link now appears and points to https://www.swift.com/payments.
    3. Open the Methodology view; confirm the copy reads "Of 75 previously-dangling source references, 27 now resolve ... the remaining 48 ... stay honestly at the Unknown floor."
    4. Spot-check an orphan-backed figure (e.g. a Novo Nordisk sustainability or an Amgen/TXN/KLAC/TMO/GILD products figure) still shows the Unknown floor (no fabricated link).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (e.g. wrong link target, orphan showing a fabricated source, methodology numbers off).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner shell → GITHUB_OUTPUT | The timestamp echo writes a step output consumed by later steps |
| static methodology copy → DOM | Disclosure text rendered in the Methodology modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | yml echo command injection | accept | The echo uses a fixed-format `$(date ...)` with no untrusted input; the fix only corrects quoting and proper redirection. No user-controlled interpolation. |
| T-12-04 | Information disclosure | methodology copy overstating resolution | mitigate | Copy and tests pin the exact honest counts (75/27/48); zero-fabrication enforced by plan 12-01's gate. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase installs zero packages (buildless, node:test built-in). |
</threat_model>

<verification>
- `npm test` green (313 baseline + new integrity/workflow cases; methodology-wiring with 75/27/48).
- `git diff` on auto-update-data.yml shows only line 34; cron/dispatch/validation/deploy untouched.
- `git diff --stat data` empty.
- Human-verify checkpoint approved (resolved SWIFT links render; orphans stay Unknown; methodology numbers correct).
</verification>

<success_criteria>
- Methodology view documents 27 resolved + 48 remaining-Unknown of 75 (INTG-02); methodology-wiring pins all three.
- auto-update-data.yml timestamp echo corrected; workflow valid; pipeline steps unchanged (INFRA-01); workflow-timestamp-quote test green.
- Full suite green; data/ contract intact; human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/12-source-fk-integrity-workflow-fix/12-02-SUMMARY.md` when done
</output>
